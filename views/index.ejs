<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Posts</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body>
    <h1 class="sticky-top bg-body p-2 bg-opacity-75" style="color: #6f42c1;">Bloggers</h1>
    <div class="container-fluid">
        <% for(let post of posts) { %>
            <div class="card post mb-3">
                <h5 class="card-header user text-primary-subtle"> @<%= post.username %></h5>
                <div class="card-body">
                    <p class="content card-text text-wrap"><%= post.content %> </p>
                    <a class="card-link btn btn-outline-info btn-sm" href="http://localhost:8080/posts/<%=post.id %>">Show more</a>
                    <a class="card-link btn btn-outline-success btn-sm" href="http://localhost:8080/posts/<%=post.id %>/edit">Edit</a>
                    <form class="d-inline card-link" action="/posts/<%= post.id %>?_method=DELETE" method="post">
                        <button class="btn btn-outline-danger btn-sm">Delete Post</button>
                    </form>
                </div>
            </div>
        <% } %>
        <br><br>
        <footer class="navbar sticky-bottom bg-body-info">
            <a class="btn btn-outline-primary btn-sm" href="http://localhost:8080/posts/new">+ Add New Post</a>
             <!-- <button id="newPost">Add New Post</button> -->
        </footer>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>